# BIM Extraction Worker Dependencies
# pdal, ifcopenshell, numpy, scipy, shapely installed via conda-forge in Dockerfile
boto3==1.35.99
botocore==1.35.99
supabase==2.10.0
httpx>=0.26,<0.28
pydantic>=2.0,<3.0
ezdxf==1.3.4
# cloud2bim installed separately if available; worker falls back to heuristic segmentation
